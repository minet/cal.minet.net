[project]
name = "media-processor"
version = "1.0.0"
description = "Media transcoding microservice for Calend'INT"
requires-python = ">=3.11"
dependencies = [
    "Pillow>=10.0.0",
    "psycopg2-binary>=2.9.0",
    "minio>=7.2.0",
]

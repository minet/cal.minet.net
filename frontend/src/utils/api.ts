/**
 * Raw axios instance for backward-compatible usage in Vue components.
 * New code should prefer the typed client: import { api } from '@/api'
 */
export { httpClient as default } from '../api/client'
